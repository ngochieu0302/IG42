@model FDI.Simple.ModelCustomerRewardItem
@using FDI.CORE
    @{
        Layout = "~/Views/Shared/_Ajax.cshtml";
        var stt = 1;
    }
    <div id="mygird">
        <div class="box-control">
            <div class="scrollable box-control-content">
                <table class="gridView table table-striped table-bordered table-hover table-orimid" role="grid">
                    <thead>
                        <tr>
                            <th class="act_roles">
                                <input value="" type="checkbox" />
                            </th>
                            <th>ID</th>
                            <th><a href="#Field=CustomerName">Khách hàng</a></th>
                            <th><a href="#Field=PriceReward">Tổng tích lũy</a></th>
                            <th><a href="#Field=PriceReceive">Tổng đã rút</a></th>
                            <th><a href="#Field=Price">Tích lũy hiện còn</a></th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.ListItems != null)
                        {
                            foreach (var item in Model.ListItems)
                            {
                                <tr data-id="@item.ID">
                                    <td class="act_roles">
                                        <input type="checkbox" value="@item.ID" class="check">
                                    </td>
                                    <td>@item.ID</td>
                                    <td>@item.CustomerName</td>
                                    <td class="text-right">@item.PriceReward.Money()</td>
                                    <td class="text-right">@item.PriceReceive.Money()</td>
                                    <td class="text-right">@(item.PriceReward - item.PriceReceive)</td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <div class="footer-control">
            @if (Model.PageHtml != null)
            {
                @Html.Raw(Model.PageHtml)
            }
        </div>
    </div>
    <script type="text/javascript">
        $(document).ready(function () {
            registerGridView('#mygird');
        });
    </script>
