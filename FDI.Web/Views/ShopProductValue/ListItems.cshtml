@model FDI.Simple.ModuleShopProductValueItem
@{
    Layout = "~/Views/Shared/_Ajax.cshtml";
    var i = 1;
}
<div id="mygird">
    <div class="box-control">
        <div class="scrollable box-control-content">
            <table class="gridView table table-striped table-bordered table-hover table-orimid" role="grid">
                <colgroup>
                    <col class="colgroup check" />
                   
                    <col class="colgroup size" />
                     <col />
                    <col class="colgroup size" />
                    <col class="colgroup size" />
                    <col class="colgroup size" />
                    <col class="colgroup action" />
                </colgroup>
                <thead>
                <tr>
                    <th class="act_roles">
                        <input value="" type="checkbox" class="checkAll" />
                    </th>
                    <th><a href="#Field=Name">STT</a></th>
                    <th><a href="#Field=Name">Tên Nguyên liệu</a></th>
                    <th><a href="#Field=UnitName">Đơn vị</a></th>
                    <th><a href="#Field=QuantityDay">HSD/ Ngày</a></th>
                    <th class="text-right"><a href="#Field=Price">Giá nhập mới nhất</a></th>
                    <th class="text-right">Thao tác</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var item in Model.ListItems)
                {
                    <tr data-id="@item.ID" title="@item.Name">
                        <td class="act_roles">
                            <input type="checkbox" value="@item.ID" class="check">
                        </td>
                        <td>@(i++)</td>
                        <td>@item.Name</td>
                        <td>@item.UnitName</td>
                        <td>@item.QuantityDay</td>
                        <td class="text-right">@string.Format("{0:0,0}", item.Price)</td>
                        <td class="text-right">
                            @Grid.ActionEdit(item.ID, item.Name)
                            @Grid.ActionDelete(item.ID, item.Name)
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
    <div class="footer-control">
        @if (Model.PageHtml != null)
        {
            @Html.Raw(Model.PageHtml)
        }
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        registerGridView('#mygird');
    });
</script>
