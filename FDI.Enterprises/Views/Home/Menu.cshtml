<div class="tabs-menu">
    <strong>Đại lý</strong>
    @*<a class="link-menu" href="/">Tổng quan</a>*@
    <a class="link-menu" href="/Supplier" >Nhà cung cấp</a>
    <a class="link-menu" href="/OrderBySupplier">Đơn hàng NCC</a>

    @*<a class="link-menu" href="/OrderByAgency" >Thống kê đơn hàng</a>
    <a class="link-menu" href="/GeneralMonthAgency" >Thống kê Doanh số</a>*@
</div>


<script>
    $(function () {
        setNavigation();
    });
    function setNavigation() {
        var path = window.location.pathname;
        path = path.replace(/\/$/, "");
        path = decodeURIComponent(path);

        $(".tabs-menu a").each(function () {
            var href = $(this).attr('href');
            if (path === href) {
                $(this).addClass('active');
                $(".tabs-menu a:nth-child(2)").removeClass("active");
            }
            else if ($(this).attr('href') === "/") {
                $(".tabs-menu a:nth-child(2)").addClass('active');
             }
        });
    }
</script>